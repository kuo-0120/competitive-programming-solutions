#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t --){
        int a, b;
        cin >> a >> b;
        cout << ((a - 2 + 2) / 3) * ((b - 2 + 2) / 3) << endl;
    }
    return 0;
}
